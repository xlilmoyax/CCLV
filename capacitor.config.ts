import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mantenimiento.cit',
  appName: 'Mantenimiento CIT',
  webDir: 'dist'
};

export default config;
